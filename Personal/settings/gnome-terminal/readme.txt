# Necessary in the Gnome terminal:
# Create a new profile before running the gnome-terminal-themes.sh script.
# Otherwise the script won't work.
export TERMINAL=gnome-terminal
./gnome-terminal-themes.sh

# Necessary in the Alacritty terminal:
# Installing the requirements is necessary in the Alacritty terminal.
export TERMINAL=alacritty
./gnome-terminal-themes.sh
